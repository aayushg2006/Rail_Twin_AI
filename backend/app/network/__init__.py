"""Vasai Road network — a 1:1 port of the frontend src/twin topology, geometry,
fleet and scenarios. These values are the single source of truth the backend
simulation shares with the SVG the console renders (1 map unit = 10 m)."""
