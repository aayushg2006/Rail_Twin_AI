"""Phase 4 — machine-learning prediction layer (ETA, delay, conflict probability).
Genuinely trained on synthetic episodes generated by the digital twin, evaluated
against baselines, with confidence + feature-contribution explainability. ML never
bypasses rule-based safety; low-confidence output falls back to the deterministic
projection."""
